# dft_pyscf

Foreign-language worker placeholder for `chimiaclaw-skill-subprocess`.

Protocol: stdin receives canonical request bytes, stdout returns canonical response bytes. Phase 0 keeps this as a stub so the Rust workspace can compile independently.
